export enum TicketStatusValue {
  SUBMITTED = "Submitted",
  IN_PROGRESS = "In-Progress",
  IN_REVIEW = "In-Review",
  RESOLVED = "Resolved",
}

export enum TicketStatusDisplay {
  ALL = "All",
  SUBMITTED = "Submitted",
  IN_PROGRESS = "In progress",
  IN_REVIEW = "In review",
  RESOLVED = "Resolved",
}

export enum TicketDateOrder {
  NEWEST = "Newest",
  OLDEST = "Oldest",
}

export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}
